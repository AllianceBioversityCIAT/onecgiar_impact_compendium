----------------------------------------------------------------------
TASK 4 — SERVER-DRIVEN SORTING (Year, Category) with URL sync
Save as: planning/re-design Frontend/sprint_dashboard_task4_server_sort.txt
----------------------------------------------------------------------

ROLE: Sr. Frontend Developer (awslabsfrontend-mcp-server)
GOAL: Implement column sorting powered by the server and coordinated with search + pagination.

## IMPLEMENTATION COMPLETED ✅

### 1. Table Header Sorting UI ✅
**File:** `src/components/ui/Table.tsx`
- Added `SortState` interface with field and direction
- Added `sort` and `onSortChange` props to TableProps
- Implemented sortable column headers as focusable buttons
- Added up/down arrow icons with active/inactive states
- Keyboard support: Enter/Space toggles sort direction
- ARIA support: `aria-sort` attribute reflects current state

**Key Features:**
- Visual indicators: ▲▼ icons show sort state
- Hover effects on sortable columns
- Proper focus management and keyboard navigation
- Accessibility compliant with ARIA attributes

### 2. Dashboard Integration ✅
**File:** `src/pages/Dashboard.tsx`
- Added `handleSortChange` function that resets to page 1
- Updated URL state management to include sort parameter
- Sort format: `sort=field:dir` (e.g., `sort=year:desc`)
- Integrated with existing search and pagination
- Updated Table component usage with sort props

**URL State Management:**
- Persists sort in URL: `?sort=year:desc`
- Restores sort state on page refresh/navigation
- Combines with search, pagination, and filters
- Resets to page 1 when sort changes

### 3. API Contract Implementation ✅
**Backend Files:** `app/routers/studies.py`
- Added `sort` parameter to both `/studies` and `/studies/search` endpoints
- Implemented field mapping: `year`, `title`, `category`
- Server-side ORDER BY clause generation
- Fallback to default ordering for invalid sort formats

**Mock Data Support:**
- Mock service already supported sorting (no changes needed)
- Client-side sorting for development/testing parity

### 4. Sortable Columns ✅
- **Year**: Numeric sorting (asc/desc)
- **Title**: String sorting (asc/desc) 
- **Category**: String sorting (asc/desc)
- Non-sortable columns: ID, Period, Summary, Contributors

## ACCEPTANCE CRITERIA VERIFICATION ✅

✅ **Clicking Year or Category header toggles asc/desc; icon state reflects sort**
- Implemented toggle logic in `handleSortClick`
- Visual feedback with arrow icons
- State properly reflected in UI

✅ **URL includes ?sort=; refresh/back restores sort**
- URL state management implemented
- Browser navigation preserves sort state
- Page refresh restores sort from URL

✅ **Sorting resets to page=1 and composes with q and pageSize**
- `handleSortChange` resets page to 1
- Combines with search query and page size
- All parameters work together

✅ **A11y: aria-sort on active column, headers are focusable buttons**
- `aria-sort` attribute set correctly (ascending/descending/none)
- Sortable headers are focusable buttons
- Keyboard navigation with Enter/Space

## TESTS CREATED ✅

### Table Component Tests
**File:** `src/components/ui/__tests__/Table.sort.test.tsx`
- Sortable headers render as buttons
- onSortChange callback triggered correctly
- Sort direction toggles on repeated clicks
- ARIA attributes set properly
- Keyboard navigation works

### Dashboard Integration Tests  
**File:** `src/pages/__tests__/Dashboard.sorting.test.tsx`
- URL updates when sort changes
- Page resets to 1 when sorting
- Sort state restored from URL
- Sort combines with search and pagination

## TECHNICAL IMPLEMENTATION DETAILS

### Frontend Changes
1. **Table.tsx**: Added sorting interfaces, handlers, and UI
2. **Dashboard.tsx**: Integrated sorting with URL state management
3. **API Integration**: Updated fetch calls to include sort parameter

### Backend Changes
1. **studies.py**: Added sort parameter to endpoints
2. **SQL Queries**: Dynamic ORDER BY clause generation
3. **Field Mapping**: Frontend to database column mapping

### Sort Format
- **Client**: `{ field: 'year', dir: 'asc' }`
- **URL**: `?sort=year:asc`
- **Server**: Parsed to SQL `ORDER BY year ASC`

## INTEGRATION WITH EXISTING FEATURES ✅

- **Search**: Sort works with search queries
- **Pagination**: Sort resets to page 1, maintains page size
- **URL State**: All parameters synchronized
- **Expandable Rows**: Sort preserves expanded state if row still visible
- **Mock Data**: Client-side sorting for development parity

## STATUS: COMPLETE ✅

All deliverables implemented and tested. Server-driven sorting is fully functional with proper URL synchronization, accessibility support, and integration with existing search and pagination features.
